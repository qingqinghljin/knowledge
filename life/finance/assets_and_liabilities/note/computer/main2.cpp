#include <iostream>
#include <stdio.h>
int main()
{
  using std::cin;
  using std::cout;
  using std::endl;
  printf("\n");
  double con_down;
  double my_down;
  double max_down;
  double all_down;
  double principal;
  double investment_amount;
  do{
    max_down = con_down + my_down;
    if (con_decline < -5.0 and max_dwon < -5.0)
      break;
    if (my_down < 0.0) {
      all_down = max_down * 4.0; 
    } else {
      all_down = con_down * 2.0;
    }
    investment_amount = principal * down;
  }while(false);
}
