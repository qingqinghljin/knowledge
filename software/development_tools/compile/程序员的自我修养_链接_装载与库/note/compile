1.为了避免冗余，往往会把一些公有的头文件放在include文件里供分布在多个不同目录的源文件调用。也会把一些头文件放在系统的include目录中。这样不用每个用到这个头文件的目录都要把这个头文件放在当前目录。
2.为了避免冗余，假设多个目录都用到了库函数，只要包含它的头文件就行，而不用把这个头文件对应的源文件也放在自己的目录中一起编译，而是通过-L或-l告诉编译器，这个源文件对应的o文件在哪里。